{
  "name": "revcover",
  "private": true,
  "scripts": {
    "dev": "concurrently -k -n API,UI -c green,cyan \"pnpm --filter api run dev\" \"pnpm --filter ui run dev\"",
    "build": "pnpm --filter @revcover/ui build",
    "preflight": "tsx scripts/preflight.ts",
    "guardrail": "pnpm -w run typecheck && pnpm -w run lint && pnpm -w run test && pnpm -w run preflight",
    "typecheck": "pnpm -r --parallel run typecheck",
    "lint": "pnpm -r --parallel run lint",
    "test": "pnpm -r --parallel run test",
    "dev:reset": "npx kill-port 3000 3001 && pnpm run dev"
  }
}