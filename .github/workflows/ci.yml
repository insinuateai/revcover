name: CI (lightweight)
on:
  push:
  pull_request:

jobs:
  ok:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: node -v && echo "✅ Lightweight CI passed. Vercel handles real builds."
